# docs / media

Drop README media here, then wire it up in the main READMEs.

The top-of-README "What it looks like" example is a static text block (no recording needed). These
images are **optional** extras if you ever want them:

| File | What to capture | Used in |
| --- | --- | --- |
| `search-before-after.png` | Side-by-side: raw grep output (huge) vs the token-capped `search_symbol` result. | optional, Performance section |
| `references-filelist.png` | A `find_references` result — the compact `file:line` jump list (sanitized — no internal paths/symbols). | optional, Usage section |

Keep media **sanitized**: no internal file paths, class/symbol names, or company/project identifiers
(see the repo's no-proprietary rule). Use a throwaway sample project for recordings.
